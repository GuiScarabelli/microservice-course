name: Continuous Integration with Github

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Setup Java JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Build Eureka Naming Server Container Image
        working-directory: ./Dockerizing/10-naming-server
        run: |
          mvn spring-boot:build-image -DskipTests
          docker tag naming-server:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/naming-server:0.0.1-SNAPSHOT

      - name: Build API Gateway Container Image
        working-directory: ./Dockerizing/10-api-gateway
        run: |
          mvn spring-boot:build-image -DskipTests
          docker tag api-gateway:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/api-gateway:0.0.1-SNAPSHOT

      - name: Build Cambio Microservice Jar
        working-directory: ./Dockerizing/10-cambio-service
        run: mvn clean package -DskipTests

      - name: Build Book Microservice Jar
        working-directory: ./Dockerizing/10-book-service
        run: mvn clean package -DskipTests
        
      - name: Docker Compose Build
        working-directory: ./Dockerizing
        run: docker-compose build

      - name: Push to Docker Hub Container Registry
        run: |
          docker tag naming-server:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/naming-server:${{github.run_id}}
          docker tag naming-server:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/naming-server:latest
          docker tag api-gateway:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/api-gateway:${{github.run_id}}
          docker tag api-gateway:0.0.1-SNAPSHOT ${{ secrets.DOCKERHUB_USERNAME }}/api-gateway:latest
          docker tag cambio-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/cambio-service:${{github.run_id}}
          docker tag cambio-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/cambio-service:latest
          docker tag book-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/book-service:${{github.run_id}}
          docker tag book-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/book-service:latest
          
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/naming-server:${{github.run_id}}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/naming-server:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/api-gateway:${{github.run_id}}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/api-gateway:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/cambio-service:${{github.run_id}}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/cambio-service:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/book-service:${{github.run_id}}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/book-service:latest
